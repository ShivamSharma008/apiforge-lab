"""Command-line entry points for the assistant.

* ``python -m assistant.cli.index``  — build/refresh the RAG index
* ``python -m assistant.cli.serve``  — run the FastAPI server
* ``python -m assistant.cli.chat``   — interactive streaming terminal chatbot
"""
